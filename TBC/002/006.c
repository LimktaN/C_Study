#include <stdio.h>

int main()
{
    printf("\"The truth is ...\nI am Ironman.\"\n");
    // escape sequence

    int x, y, z;

    x = 1;
    y = 4;
    z = x + y;

    printf("%i + %i = %i.", x, y, z);

    return 0;
}